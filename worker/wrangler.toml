# Cloudflare Worker config for the submission proxy.
# Deploy from this directory with `wrangler deploy`.
# Secrets (GITHUB_TOKEN, TURNSTILE_SECRET) are set with `wrangler secret put`,
# never in this file.
name = "cla-submit"
main = "src/index.js"
compatibility_date = "2024-11-01"
